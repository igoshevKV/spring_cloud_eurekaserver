spring:
  application:
    name: eureka-server
  profiles:
    active: default
  cloud:
    config:
      uri: http://192.168.0.207:8071
    loadbalancer:
      ribbon:
        enabled: false
server:
  port: 8070